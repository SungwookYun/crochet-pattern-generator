{
  "name": "crochet-pattern-generator",
  "version": "1.0.0",
  "description": "Crochet pattern generator",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && CI=false npm run build",
    "vercel-build": "cd client && npm install && CI=false npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "dotenv": "^16.0.3",
    "openai": "^4.20.1",
    "node-cron": "^3.0.2",
    "path": "^0.12.7"
  },
  "engines": {
    "node": "18.x"
  }
} 